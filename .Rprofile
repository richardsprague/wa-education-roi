# Keep the project self-contained and quiet.
options(
  repos = c(CRAN = Sys.getenv("CRAN_SNAPSHOT", "https://cloud.r-project.org")),
  stringsAsFactors = FALSE,
  dplyr.summarise.inform = FALSE,
  readr.show_progress = FALSE
)
if (interactive()) {
  message("wa-education-roi -- see R/00_config.R for the analysis knobs.")
}
